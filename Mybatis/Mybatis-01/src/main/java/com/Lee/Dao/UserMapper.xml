<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespcae绑定一个对应的Dao、Mapper接口-->
<mapper namespace="com.Lee.Dao.UserMapper">
    <!--select查询语句-->
    <select id="getUserList" resultType="com.Lee.pojo.User">
        select * from mybatis.user
    </select>

    <select id="getUserById" resultType="com.Lee.pojo.User" parameterType="int">
        select * from mybatis.user where id = #{id}
    </select>

    <!--对象中的属性可以直接取出来-->
    <insert id="addUser" parameterType="com.Lee.pojo.User">
        insert into mybatis.user (id, name, pwd) VALUE (#{id},#{name},#{pwd})
    </insert>

    <!--传递map的key-->
    <insert id="addUser2" parameterType="map">
        insert into mybatis.user (id, name, pwd) VALUE (#{userid},#{username},#{pwd})
    </insert>

    <update id="updateUser" parameterType="com.Lee.pojo.User">
        update mybatis.user
        set name = #{name},pwd = #{pwd}
        where id = #{id};
    </update>

    <delete id="deleteUser" parameterType="int">
        delete from mybatis.user where id = #{id}
    </delete>
</mapper>